---
applications:
  - name: demogit1
    path: target/demogit1-0.0.1-SNAPSHOT.war
    memory: 1024M
    instances: 1
    random-route: true

    buildpacks:
      - sap_java_buildpack

    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 17.+ } }'